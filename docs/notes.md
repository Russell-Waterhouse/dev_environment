# Neovim Notes
## A small list of things I've recently learned

- You can open a URL in the browser, or a file with `gx`

  - https://example.com
  - ~/.bashrc

- `gJ` joins lines like `J` does but doesn't insert the space

A line <>
A second line <>


